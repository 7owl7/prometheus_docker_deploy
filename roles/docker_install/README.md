docker_install
=========

This role install docker on an ubuntu host

Example Playbook
----------------

The role usage example: 
```yaml
    - hosts: servers
      roles:
         - role: docker_install
```
